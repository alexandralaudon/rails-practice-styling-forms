<%= form_for(@person, html: {class: "form-horizontal"}) do |f| %>
  <% if @person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@person.errors.count, "error") %> prohibited this person from being saved:</h2>

      <ul>
      <% @person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :first_name, class: "col-md-2 control-label" %>
    <div class="col-md-6">
      <%= f.text_field :first_name, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :last_name, class: "col-md-2 control-label" %>
    <div class="col-md-6">
      <%= f.text_field :last_name, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :date_of_birth, class: "col-md-2 control-label" %>
    <div class="col-md-3">
      <%= f.date_field :date_of_birth, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :bio, class: "col-md-2 control-label" %>
    <div class="col-md-6">
      <%= f.text_area :bio, class: "form-control" %>
    </div>
  </div>

    <!-- <div class="form-group">
      <div class="col-md-4">
        <ul><%= f.label :first_name %>
        <%= f.text_field :first_name %></ul>
      </div>
    </div>
  -->

    <div class="form-group col-md-3">
        <ul><%= f.check_box :active %>
        <%= f.label :active %></ul>
    </div>

    <br>

    <div class="row">
      <div class="col-md-3 col-md-offset-1">
        <%= link_to "Create Person", "#", class: "btn btn-primary" %>
        <!-- This creates a button, link_to (argument on page, named route, HTML argument) -->
        <%= link_to "cancel", "#", class: "btn btn-default", data: {confirm:"Are you sure you want to cancel?"} %>
        <!-- This creates a button, but has a CONFIRM message -->
      </div>
    </div>



  <!-- <div class="actions">
    <%= f.submit %>
  </div> -->



<% end %>
